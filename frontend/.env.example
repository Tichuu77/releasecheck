# Copy to .env.local and set your API base URL.
# In local dev with Vite proxy, this can be left empty (defaults to /api).
# For production or when running the API on a different origin, set it explicitly.

VITE_API_URL=/api
